# What I Learned

- React Native uses native views instead of HTML on Android and iOS.
- `View` groups content; `Text` displays it.
- `Image` shows pictures, and `TextInput` lets users type.
- `Pressable` handles presses; `ScrollView` enables scrolling.
- `Platform.OS` identifies where the app is running.