# What I Learned — Lists & Scrolling Data

## Lesson recap

- FlatList receives data and uses renderItem to display each row.
- keyExtractor should return a stable, unique ID.
- Pagination adds a batch; refreshing reloads the starting data.
- Loading, empty results, and errors need different feedback.