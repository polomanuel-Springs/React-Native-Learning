# What I Learned

- I use AsyncStorage for preferences and SecureStore for native tokens.
- Saving a token lets me restore a session after a restart.
- A protected screen checks the session, and the server must validate real requests.
