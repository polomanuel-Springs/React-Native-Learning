# What I Learned 

- src/app contains routes; each training folder contains a focused example.
- Public environment variables are included in the app and cannot hold secrets.
- TypeScript checks types, ESLint checks code rules, and Prettier formats code.

